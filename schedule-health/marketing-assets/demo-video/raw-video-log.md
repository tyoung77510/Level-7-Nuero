# Raw LinkedIn Video — rotation and log

> **Status:** ✅ Active (5-day trial) | **Purpose:** Dedup ledger and topic rotation for the raw video routine. The routine reads this file first on every run to find the next topic and to check whether the 5-video trial is complete. | **Last Updated:** 2026-07-30

**Rule for the routine:** never re-record a topic already logged below. Take the next unlogged row from the rotation table, in order. If 5 rows are logged, disable the trigger and stop — do not produce a 6th. **Status as of 2026-07-30: 4 of 5 produced, out of order** — #4 (Ask Ordo) is blocked on a missing `ANTHROPIC_API_KEY` and remains outstanding; #5 (Finish forecast) was produced instead per Taj's explicit choice. Do not disable the trigger until #4 is actually done.

## Rotation (planned topics — verify live before recording, don't record from memory)

| # | Topic | What it demonstrates | Verify before recording |
|---|---|---|---|
| 1 | Upload → health score | Empty upload screen, Load Sample Data, real score + flagged issues landing | Already produced (see log below) |
| 2 | Issue Punch List + "How to fix" | Clicking a real flagged issue's "How to fix" guidance | Already produced (see log below). Note for future local recordings: `fixGuidance` is gated to Professional/Team/Enterprise — a fresh local demo account defaults to Free and needs its `plan_tier` upgraded (direct DB write is fine for this local, non-production instance) before "How to fix" will show real guidance instead of an upgrade prompt. |
| 3 | What-if Sandbox | Adjusting float/duration on an activity and watching the health score recalculate live, before committing anything | Confirm the Sandbox tab is reachable on the demo/admin account's current tier and actually recalculates in the browser (client-side, per `index.html`'s `sandboxScoreFrom`) |
| 4 | Ask Ordo | Typing a real plain-language question and getting a real AI-grounded answer back | Requires `ANTHROPIC_API_KEY` configured and AI credits available on the account used — confirm a real answer comes back (not a 429/503) before recording; if AI is unavailable, stop and flag rather than substitute a different topic silently. **Blocked as of 2026-07-30**: local `.env` has no real `ANTHROPIC_API_KEY` set (only the `.env.example` placeholder). Flagged to Taj rather than substituting silently; he chose to produce #5 out of order instead and come back to this one once a real key is available. |
| 5 | Finish forecast (Earned Schedule) | The two-scenario IEAC(t) finish-date forecast card on a schedule with real SPI/progress data | Already produced (see log below), out of order — #4 is blocked (above). Confirmed `finishForecast.available` was false for the built-in sample data (no `target_start_date`/`target_end_date` columns at all) via a direct `/api/analyze` call before recording, per this rule; built a different, real test dataset instead (see note below) that genuinely qualifies. |

## Log (produced videos)

| # | Date | Topic | File | Caption | Posted? |
|---|---|---|---|---|---|
| 1 | 2026-07-26 | Upload → health score | `ordo7-raw-01-upload-to-score.mp4` | "I ran a real project schedule through this. Failed 5 checks. Instantly." / real flagged issues / "This is Ordo7." / "Try it free — link in the comments." (no closing question — produced before this SOP's question requirement; later entries should end on a real question instead of a flat CTA line) | **Posted 2026-07-27, 9:28 AM** — [post](https://www.linkedin.com/posts/taj-young_projectmanagement-schedulerisk-constructionmanagement-ugcPost-7487544355545718784-J-Z4) (hashtags added at publish time, not part of the original caption). Real analytics pulled shortly after posting: 18 impressions, 11 reached, 6 video views, 22s avg watch time, 1 comment — **that comment is Taj's own first-comment CTA link, not an organic reply**, so organic engagement so far is actually 0/0/0/0 (reactions/comments/reposts/saves), same pattern as everything else tracked. Sample is tiny and very early (pulled roughly an hour after posting) — too early to read anything into the reach numbers or conclude the format isn't working yet. |

| 2 | 2026-07-28 | Issue Punch List + "How to fix" | `ordo7-raw-02-issue-fix-guidance.mp4` | "A schedule fails 5 checks." / "Ordo7 doesn't just flag it — it tells you how to fix each one." / "Real, specific guidance per issue. Not one generic tip." / "What's the first fix you'd make here?" | **Posted 2026-07-28** (confirmed by Taj on 2026-07-29) — exact post time/URL not captured; add here if/when Taj shares the link. |
| 3 | 2026-07-29 | What-if Sandbox | `ordo7-raw-03-sandbox.mp4` | "A schedule scored 2 out of 100." / "The Sandbox tests recovery scenarios live -- before you touch anything real." / "Fix the float. Fix the duration. Watch the score react instantly." / "What's the first fix you'd try on a 2 out of 100 schedule?" | not yet — staged, awaiting manual post |
| 5 | 2026-07-30 | Finish forecast (Earned Schedule) | `ordo7-raw-05-forecast.mp4` | "A real schedule. Health score: 50 out of 100." / "Planned progress: 90.5%. Actual: 54.1%. SPI 0.60 -- already behind." / "Ordo7 doesn't stop at a score -- it forecasts the finish date two ways." / "At current pace: Jan 2027. Back on plan: Nov 2026. Your call?" | not yet — staged, awaiting manual post |

**Note on entry 1:** produced during the same session that diagnosed Day 009 and designed this format, before the formal SOP (this file, `raw-linkedin-video-sop.md`) was written down. Its caption doesn't yet end on a question, unlike the blueprint this SOP locks in — future entries (2 onward) should. Not re-cut retroactively; it's still a valid, real, un-fabricated raw video and is fine to post as-is if the owner wants variety in what "close" looks like, but the routine's own output going forward should follow the SOP exactly.

**Note on entry 2:** produced manually (not by the automated trigger, which had not fired as of request time) after verifying live that "How to fix" needed a Professional-tier account to show real guidance — confirmed non-generic, issue-specific content (negative float vs. long-duration guidance differ substantively) before recording, per the SOP's hard rule. Owner asked for bigger, bolder captions after seeing the first cut — re-rendered with `FontSize` 34→46 and caption color changed from white to Ordo7 brand orange `#f37443`. Owner then asked for a more eye-appealing, typewriter-style font — switched from DejaVu Sans to Courier 10 Pitch Bold (monospace typewriter look). Owner then asked for the captions to go bigger still, wrap to two lines rather than shrink, actually animate letter-by-letter typewriter-style so they draw the eye, and switch color to yellow so they never wash out against the app UI. Rebuilt as a real ASS subtitle file (`ordo7-raw-02-captions.ass`, committed alongside the video as the source for this styling) with a Dialogue event per revealed character, a blinking-cursor `_` trailing during typing, `FontSize` 64, `PrimaryColour` yellow `&H0000FFFF`, and a near-opaque black background box (`BackColour &HE0000000`) behind the text for contrast. Same 12.4s cut and overall caption beat timing throughout, styling/animation only. Full caption text for the LinkedIn post:

> A schedule fails 5 checks in seconds. That's not the interesting part.
>
> Ordo7 doesn't stop at flagging it — click "How to fix" on any issue and it gives you real, specific guidance for that exact problem. Negative float gets a different answer than a 63-day activity with no checkpoints. Not one canned tip copy-pasted across every issue type.
>
> This is Ordo7: upload a schedule, see what's actually wrong, know what to do about it.
>
> What's the first fix you'd make on a schedule like this — the negative float, or the missing logic?
>
> #Ordo7 #ProjectControls #Scheduling #ConstructionManagement

First comment (post immediately after publishing): `Try Ordo7 free → https://ordo7.pro`

**Note on entry 5:** produced out of order — Taj asked for "the video for today's LinkedIn post," and the next unproduced rotation slot (#4, Ask Ordo) was blocked (see rotation table above: no real `ANTHROPIC_API_KEY` configured locally). Rather than substitute a different topic silently or fabricate an AI answer, I flagged the block to Taj; he chose to move to #5 instead. Verified live before recording, per the SOP's hard rule: the built-in sample data has no `target_start_date`/`target_end_date` columns at all, so `finishForecast.available` came back `false` on a direct `/api/analyze` call — it genuinely cannot demo this feature. Built a different, real `.xer` test file instead (`ordo7-raw-05-sample-data.xer`, committed alongside the video) with real planned/baseline dates (project window 2026-01-05 to 2026-09-01) and real `phys_complete_pct` progress values on each activity — same named activities as the earlier videos (Mobilize site, Foundation pour, Steel delivery, Roofing, MEP rough-in, Final inspection) for visual continuity across the trial, but scoped to a full multi-month project instead of the short original sample. Uploaded through the real file-upload input (not injected via JS) and confirmed live via the actual API response before recording: health score 50/100, Earned Schedule planned progress 90.5% vs. actual 54.1% (SPI 0.60, genuinely behind), and a real two-scenario finish forecast — at current pace 2027-01-20, if back on plan 2026-11-11, against a planned finish of 2026-08-20. All numbers in the caption match that real API response exactly; nothing was rounded up or invented. Same visual treatment as entries 2-3: Courier 10 Pitch Bold, FontSize 64, yellow typewriter reveal (`ordo7-raw-05-captions.ass`, committed alongside). **Updated same day:** owner asked for the video to run 14 seconds and for the zoom to be a smooth continuous push-in rather than the pulsing in/out used on entry 3 — rebuilt the `zoompan` expression as a monotonic staircase (four smoothstep-eased holds at 1.00 → 1.05 → 1.10 → 1.15 → 1.20, each easing in over ~0.6-0.8s then holding, never zooming back out) instead of the raised-cosine bumps that return to baseline. Extended runtime from 12.0s to exactly 14.0s by lengthening the final `tpad` hold (0.68s → 2.68s) rather than changing any caption timing — the extra hold is on an already-static screen (nothing animates there), so it's a longer look at real content, not fabricated motion.

**Update to entry 3 (same day):** owner asked for zoom in/out effects on the screen recording and for the video to run 12 seconds. Added four subtle Ken Burns-style zoom punches (peaking at ~1.12x, eased in/out with a raised-cosine curve) timed to each of the four caption beats, via ffmpeg's `zoompan` filter (not `crop` — tried crop's dynamic w/h expressions first, but crop only evaluates output width/height once at init, not per-frame, so it produced no visible zoom; `zoompan` re-evaluates zoom per output frame and is the correct tool for this). Extended runtime from 11.88s to exactly 12.0s by cloning the final (already-captioned) frame for an extra 0.12s via `tpad`, rather than changing any real timing. Same source recording, same edits, same score progression (2 → 30) — visual polish only.

**Note on entry 3:** produced manually per Taj's request ("we need a video and caption for today"). Verified live before recording, per the SOP's hard rule: confirmed the demo account (still Professional tier from entry 2's DB change) can reach the Sandbox tab, and that `checkFlagEnabled('sandbox-simulator')` fails open (flags default to enabled unless explicitly turned off server-side) — so the tab renders real controls, not an upgrade/unavailable message. Recorded three genuine edits against the built-in sample data's real activities: Steel delivery's float (-1 → 3, clears the one negative-float issue the sandbox can affect), Roofing's float (50 → 20, clears its excessive-float risk), and MEP rough-in's duration (62.5 → 30, clears its long-duration risk) — all three are real, live inputs into the same `sandboxScoreFrom`/`sandboxContribution` formula documented in `index.html`, not scripted/faked numbers. The score moved 2 → 30 (+28), confirmed via frame capture at each step, matching a hand-computed check of the formula before recording. Deliberately did **not** claim the sandbox reaches "DCMA compliant" or any dramatic before/after framing — with this sample data the other 4 critical + 1 at-risk issues (missing logic, hard constraints, out-of-sequence, invalid dates) aren't editable via float/duration, so 30/100 is the honest ceiling here, not a stronger number. Styling matches entry 2's final version exactly (`ordo7-raw-03-captions.ass`: Courier 10 Pitch Bold, FontSize 64, yellow `&H0000FFFF`, near-opaque black box, letter-by-letter typewriter reveal with cursor) for visual consistency across the trial. Full caption text for the LinkedIn post:

> A schedule scored 2 out of 100. That's a real, flawed sample schedule — not a hypothetical.
>
> Ordo7's What-If Sandbox lets you test recovery scenarios live, before you touch anything real: fix a negative-float item, bring an over-extended float back into range, shorten a duration that's run too long — and watch the health score react instantly, right in the browser. Nothing is saved until you decide it should be.
>
> This is Ordo7: see what's wrong, test the fix, know it works before you commit to it.
>
> What's the first fix you'd try on a schedule scoring 2 out of 100 — the float, or the duration?
>
> #Ordo7 #ProjectControls #Scheduling #ConstructionManagement

First comment (post immediately after publishing): `Try Ordo7 free → https://ordo7.pro`
